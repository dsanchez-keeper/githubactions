on:
  #push:
    #branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      
      - name: Retrieve secrets from Keeper
        id: ksm
        uses: Keeper-Security/ksm-action@master
        with:
          keeper-secret-config: ${{ secrets.DEMO_SECRET }}
          secrets: |-

            ZwZ7r955CXVgqB7hWx7jqg/field/password > github mysql

      - name: Print PW Value
        run: |
          echo "password is ${{ steps.ksm.outputs.DEMOPW }}"
